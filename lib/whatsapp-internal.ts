/**
 * Private WhatsApp recipients for lead alerts.
 * Import only from server-side automation — never from public/client pages.
 */
export const SILENT_WHATSAPP_LEAD_NUMBERS = ["254706710371"] as const;
